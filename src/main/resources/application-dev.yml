server:
  port: 8080
spring:
  application:
    name: Multiple-Env-Demo
  datasource:
    url: jdbc:postgresql://localhost:5432/dev_db
    username: admin
    password: admin123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
